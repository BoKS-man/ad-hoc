1.	Есть две таблицы, T1(Id int, Quantity int) и T2(Id int, Quantity int), связаны по Id. 
	Обновить в таблице Т1 значения Quantity на соответствующие из Т2, но только для тех записей, у которых Т1.Quantity отличается от Т2.Quantity не более чем на 20%.

2.	Есть таблица T1(Id int identity (1,1), Quantity int, Data datetime). 
	Необходимо просуммировать Quantity на каждый день и все ему предшествующие (накопительный итог).

3.	Есть таблица T1(Id int, Quantity int), из двух полей – Id и количество. Она содержит несколько строк. Изменить количество в строке с Id = x на Q1 . 
	Пересчитать значение поля Quantity в остальных строках таблицы пропорционально доле к сумме этих строк, так чтобы общая сумма не изменилась. 
	Сделать это транзакционно целостно.